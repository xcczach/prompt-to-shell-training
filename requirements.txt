# Core pipeline
pyyaml>=6.0.1
httpx>=0.27.0
datasets>=2.19.0

# Training (Unsloth QLoRA + FSDP)
transformers>=4.41.2
accelerate>=0.33.0
peft>=0.11.1
trl>=0.8.6
unsloth>=2024.5.19
safetensors>=0.4.2
sentencepiece>=0.1.99
tiktoken>=0.7.0

# Serving
vllm>=0.5.0

# Note: Install a compatible PyTorch separately for your CUDA/CPU environment.
# Example: pip install torch --index-url https://download.pytorch.org/whl/cu121
